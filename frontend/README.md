# GeoWorkbench Frontend

React + TypeScript workbench for the Phase 1 borehole correction system.

## Local Run

```powershell
cd frontend
npm install
npm run dev
```

Open:

`http://127.0.0.1:5173`

The Vite dev server proxies `/api` and `/assets` to the FastAPI backend on port `8081`.
